작업들은 모두 Ctrl + ` 을 사용하여 터미널에서 실행한다
cd back 사용하여 back 풀더로 이동하기
npm install 을 사용하여 필요한 라이브러리 등을 다운 받아야한다
ERROR가 안났을 경우에는 npm run dev 사용해서 라이브러리 실행
새로운 터미널 실행하기
cd front 사용해서 front 풀더로 이동하기
똑같이 npm install 후 npm run dev 
ERROR 없을 시 정상적으로 실행됨
